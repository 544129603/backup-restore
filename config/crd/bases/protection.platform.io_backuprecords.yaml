---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.17.3
  name: backuprecords.protection.platform.io
spec:
  group: protection.platform.io
  names:
    kind: BackupRecord
    listKind: BackupRecordList
    plural: backuprecords
    shortNames:
    - brecord
    singular: backuprecord
  scope: Cluster
  versions:
  - additionalPrinterColumns:
    - jsonPath: .status.phase
      name: Phase
      type: string
    - jsonPath: .spec.source.clusterRef
      name: Cluster
      type: string
    - jsonPath: .spec.repositoryRef.name
      name: Repository
      type: string
    - jsonPath: .spec.inventory.resourceCount
      name: Resources
      type: integer
    - jsonPath: .spec.inventory.pvcCount
      name: PVCs
      type: integer
    - jsonPath: .spec.inventory.backupBytes
      name: Bytes
      type: integer
    - jsonPath: .spec.expiresAt
      name: Expires
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            properties:
              backupID:
                type: string
              backupPath:
                type: string
              checksum:
                type: string
              checksumAlgorithm:
                type: string
              clusterRef:
                minLength: 1
                type: string
              contentCompleteness:
                type: string
              encryption:
                properties:
                  algorithm:
                    type: string
                  enabled:
                    type: boolean
                  keyRef:
                    properties:
                      key:
                        minLength: 1
                        type: string
                      name:
                        minLength: 1
                        type: string
                      namespace:
                        minLength: 1
                        type: string
                    required:
                    - key
                    - name
                    - namespace
                    type: object
                type: object
              expiresAt:
                format: date-time
                type: string
              formatVersion:
                type: string
              inventory:
                properties:
                  backupBytes:
                    format: int64
                    type: integer
                  failedResourceCount:
                    format: int64
                    type: integer
                  failedSnapshotCount:
                    format: int64
                    type: integer
                  namespaceCount:
                    format: int64
                    type: integer
                  pvcCount:
                    format: int64
                    type: integer
                  resourceCount:
                    format: int64
                    type: integer
                  snapshotCount:
                    format: int64
                    type: integer
                type: object
              operatorVersion:
                type: string
              policyRef:
                properties:
                  name:
                    minLength: 1
                    type: string
                  uid:
                    type: string
                required:
                - name
                type: object
              repositoryRef:
                properties:
                  name:
                    minLength: 1
                    type: string
                  uid:
                    type: string
                required:
                - name
                type: object
              snapshotLifecycle:
                type: string
              snapshots:
                items:
                  properties:
                    creationTime:
                      format: date-time
                      type: string
                    driver:
                      type: string
                    error:
                      type: string
                    phase:
                      type: string
                    pvcName:
                      type: string
                    pvcNamespace:
                      type: string
                    readyToUse:
                      type: boolean
                    restoreSize:
                      format: int64
                      type: integer
                    snapshotClass:
                      type: string
                    snapshotHandle:
                      type: string
                    storageClass:
                      type: string
                    volumeSnapshotContentName:
                      type: string
                    volumeSnapshotName:
                      type: string
                  required:
                  - pvcName
                  - pvcNamespace
                  type: object
                type: array
              source:
                properties:
                  clusterRef:
                    type: string
                  clusterUID:
                    type: string
                  kubernetesVersion:
                    type: string
                  namespaces:
                    items:
                      type: string
                    type: array
                  scopeMode:
                    type: string
                required:
                - clusterRef
                type: object
              sourceTaskRef:
                properties:
                  name:
                    minLength: 1
                    type: string
                  uid:
                    type: string
                required:
                - name
                type: object
            required:
            - backupID
            - backupPath
            - checksum
            - clusterRef
            - formatVersion
            - policyRef
            - repositoryRef
            - source
            - sourceTaskRef
            type: object
          status:
            properties:
              completedAt:
                format: date-time
                type: string
              conditions:
                items:
                  description: Condition contains details for one aspect of the current
                    state of this API Resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        lastTransitionTime is the last time the condition transitioned from one status to another.
                        This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        message is a human readable message indicating details about the transition.
                        This may be an empty string.
                      maxLength: 32768
                      type: string
                    observedGeneration:
                      description: |-
                        observedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      minimum: 0
                      type: integer
                    reason:
                      description: |-
                        reason contains a programmatic identifier indicating the reason for the condition's last transition.
                        Producers of specific condition types may define expected values and meanings for this field,
                        and whether the values are considered a guaranteed API.
                        The value should be a CamelCase string.
                        This field may not be empty.
                      maxLength: 1024
                      minLength: 1
                      pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                      type: string
                    status:
                      description: status of the condition, one of True, False, Unknown.
                      enum:
                      - "True"
                      - "False"
                      - Unknown
                      type: string
                    type:
                      description: type of condition in CamelCase or in foo.example.com/CamelCase.
                      maxLength: 316
                      pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                      type: string
                  required:
                  - lastTransitionTime
                  - message
                  - reason
                  - status
                  - type
                  type: object
                type: array
              createdAt:
                format: date-time
                type: string
              deletion:
                properties:
                  mode:
                    type: string
                  repositoryDataDeleted:
                    type: boolean
                  snapshotsDeleted:
                    format: int32
                    type: integer
                  startedAt:
                    format: date-time
                    type: string
                type: object
              errorCode:
                type: string
              lastRestoreTime:
                format: date-time
                type: string
              lastVerifiedAt:
                format: date-time
                type: string
              message:
                type: string
              missingSnapshots:
                items:
                  type: string
                type: array
              observedGeneration:
                format: int64
                type: integer
              phase:
                type: string
              protected:
                type: boolean
              reason:
                type: string
              restorable:
                type: boolean
              restoreCount:
                format: int64
                type: integer
              startedAt:
                format: date-time
                type: string
              verifiedFiles:
                format: int64
                type: integer
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
